<h1>投稿一覧</h1>
<% @lists.each do |list| %>
  <p>タイトル・本文</p>
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  <span><%= list.body %></span>
  <p>画像</p>
  <%= image_tag list.image, size: "200x200" %>
<% end %>